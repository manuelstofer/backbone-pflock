var myModule = require('backbone-pflock'),
    Backbone = require('solutionio-backbone');

describe('test setup', function () {
    it('should work', function () {
        true.should.not.equal(false);
    });
});

